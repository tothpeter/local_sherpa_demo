DOCKER_IMAGE_NAME="project-ruby-docker"

alias de="docker exec -it $DOCKER_IMAGE_NAME"

t() {
  # Start the container if it's not running
  if ! docker ps | grep -q "$DOCKER_IMAGE_NAME"; then
    echo "Starting the container..."
    docker-compose up -d
  fi

  de rspec $@
}
